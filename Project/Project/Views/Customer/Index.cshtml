@model IEnumerable<Project.Model.EF.KHACH_HANG>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h4>Danh sách khách hàng</h4>
<hr />
<div class="container-fluid">
    <div class="form-row">
        <div class="col-md-6" style="margin-bottom:10px">
            <a href="/Customer/Create" class="btn btn-primary"> <i class="fa fa-plus" aria-hidden="true"></i> Thêm mới</a>
            <a href="/Customer/Index" class="btn btn-info"><i class="fas fa-sync-alt"></i> Reload</a>
            @*@Html.ActionLink("Thêm mới", "Create", "Order", null, new { @class = "btn btn-primary" })*@
        </div>
        
    </div>
</div>
<div class="box-body no-padding margin-top-10">
    <div id="divResult">
        @{ Html.RenderPartial("_Index");}
    </div>
</div>
